import { HighlightSearchQueryPipe } from './highlight-search-query.pipe';

describe('HighlightSearchQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightSearchQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
